# Your notes (this file persists across restarts)

This file — `~/CLAUDE.md` (`/home/agent/CLAUDE.md`) — is **yours** and **survives restarts**.
Use it for notes, project conventions, decisions, and standing instructions you want to keep
long-term; Claude loads it automatically each session.

Your system guide and operator guidance live in `~/.claude/CLAUDE.md`, which is **regenerated
from scratch on every restart** — edits there are lost. Keep anything you want to persist here
instead (or, for operator-visible standing instructions, use `swarm_append_guidance`).

<!-- Add your own persistent notes below this line. -->
