# Single origin: open the dashboard THROUGH the gateway at http://localhost:8080.
# Empty => all gateway calls (/api/agents, /a/:id/...) are relative to the page
# origin, so they're same-origin and there's no CORS. (Opening :3000 directly
# won't reach the gateway — use :8080.)
NEXT_PUBLIC_GATEWAY_URL=
